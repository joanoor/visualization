import { withInstall } from '@/utils'
import toggleButton from './src/ToggleButton.vue'
import radioGroupButton from './src/RadioGroupButton.vue'

export const ToggleButton = withInstall(toggleButton)
export const RadioGroupButton = withInstall(radioGroupButton)
